# Frontend config.
# VITE_USE_MOCK=false  -> hit the real Express backend on :4000 (Vite proxies /api).
# unset / any other value -> use the in-browser mock engine (no backend needed).
VITE_USE_MOCK=false
